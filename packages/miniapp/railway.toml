[build]
builder = "nixpacks"
buildCommand = "cd packages/miniapp && npm ci && npm run build"

[deploy]
startCommand = "cd packages/miniapp && npm start"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

